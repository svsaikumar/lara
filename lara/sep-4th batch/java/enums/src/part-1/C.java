//package pack1;
enum C
{
	int i;
}

// empty enum is posiable
// enum with one atribute is not posiable
// without a constant what is need of output --- atributes are mainly for constant
// enum is mainly for grouping
// inside a enum without a constants we cannot develop a atribute