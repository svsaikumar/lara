interface I
{
	int m;
	
}
//as every constant is final inside interface so,it should be inititalized(final constants should be initialized at the time of declaring only)
//every consant and attribute inside interface should be initialized 